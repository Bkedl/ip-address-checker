image: php:8.3

before_script:
  - curl -sS https://getcomposer.org/installer | php
  - php composer.phar install

stages:
  - test

test:
  stage: test
  script:
    - vendor/bin/phpunit --bootstrap src/functions.php tests/TotalValidIPsTest.php

    